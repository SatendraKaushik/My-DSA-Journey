#include <iostream>
using namespace std;
int fact=1;
for( i=1,i<=n,i++) {
  fact=fact*i;
}
  int nCr (int n,int r ){
    int num = fact(n);
    int denom = fact(r)*fact(n-r);
    int ans = num/denom;
  }
int main () {
  int n,r ;
  cin n >> r ;
  cout << " Answer is " << ans << endl ;
}
